<% title "#{link_to @gallery.name, admin_gallery_path(@gallery)} &raquo; #{@picture.title}" %>

<div id="picture" class="picture">
  <div class="preview">
    <%= image_tag(@picture.image_url) %><br/>
    <span class="hint">(<%= number_to_human_size @picture.image.size %>)</span>
  </div>
</div>

<div class="info">
  <p>
    <%= link_to 'edit', edit_admin_gallery_picture_path(@gallery, @picture) %> |
    <%= link_to 'back', admin_gallery_path(@gallery) %>
  </p>

  <p><b>Description:</b></p>
  <p><%= @picture.description %></p>
  <hr class="thick" />
</div>

<div id="picture">
  <% @pictures.each do |picture| %>
    <span class="mini-pictures">
      <%= link_image(picture, @gallery) %>
    </span>
  <% end %>
</div>

